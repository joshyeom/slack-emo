---
name: web-design-guidelines
description: 웹 인터페이스 가이드라인 준수 여부를 검토하는 UI 코드 리뷰 도구. "UI 리뷰해줘", "접근성 확인해줘", "디자인 감사해줘", "UX 리뷰해줘", "베스트 프랙티스에 맞는지 확인해줘" 등의 요청 시 사용.
argument-hint: <파일-또는-패턴>
---

# 웹 인터페이스 가이드라인

웹 인터페이스 가이드라인 준수 여부를 파일 단위로 검토.

## 작동 방식

1. 아래 소스 URL에서 최신 가이드라인 가져오기
2. 지정된 파일 읽기 (또는 사용자에게 파일/패턴 요청)
3. 가져온 가이드라인의 모든 규칙에 대해 검사
4. `파일:라인` 형식으로 간결하게 결과 출력

## 가이드라인 소스

각 리뷰 전에 최신 가이드라인 가져오기:

```
https://raw.githubusercontent.com/vercel-labs/web-interface-guidelines/main/command.md
```

WebFetch를 사용하여 최신 규칙 가져오기. 가져온 콘텐츠에는 모든 규칙과 출력 형식 지침이 포함되어 있음.

## 사용법

사용자가 파일 또는 패턴 인자를 제공하면:

1. 위 소스 URL에서 가이드라인 가져오기
2. 지정된 파일 읽기
3. 가져온 가이드라인의 모든 규칙 적용
4. 가이드라인에 명시된 형식으로 결과 출력

파일이 지정되지 않으면 사용자에게 어떤 파일을 리뷰할지 질문.
